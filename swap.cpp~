#include <iostream>

int a[] = {1,2,3};

void swap(int& a, int& b) {

  int temp = a;
  a = b;
  b = a;
};
int main() {
  swap(a[0], a[2]);
  for (auto x : a)
    std::cout << a << ' ';
  return 0;
}
